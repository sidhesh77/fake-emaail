# `http-server`

HTTP API + SMTP in one binary. Env vars and production notes: repo root [`README.md`](../../README.md).

| Method | Path |
|--------|------|
| GET | `/api/health` |
| POST | `/api/temporary-address` |
| GET | `/api/inbox/poll` |
