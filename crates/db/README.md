# `db`

Postgres pool, embedded migrations (`crates/db/migrations/`), and repository helpers for `temporary_email` / `received_email`. See repo root [`README.md`](../../README.md).

`DATABASE_URL` is required for `connect_pool()`.
